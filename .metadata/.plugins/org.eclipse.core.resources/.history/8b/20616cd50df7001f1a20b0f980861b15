#ifndef __LOCK_H_
#define __LOCK_H_

#include "stdint.h"
#include "stdbool.h"

static inline bool try_accquire_lock(volatile uint8_t *lock);
static inline void accquire_spinlock(volatile uint8_t *lock);
static inline release_spinlock(volatile uint8_t *lock);



#define

#endif
